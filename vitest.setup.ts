// Registers @testing-library/jest-dom matchers on Vitest's expect and augments
// its types, so DOM assertions like toBeInTheDocument() work in component specs.
import "@testing-library/jest-dom/vitest";
